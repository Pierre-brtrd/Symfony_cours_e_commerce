{% if products|length > 0 %}
	{% for product in products %}
		{% include "Frontend/Products/_card.html.twig" with {product: product } only %}
	{% endfor %}
{% else %}
	<div class="alert alert-info mt-4 w-100 mb-4" role="alert" id="product-no-content">
		<span>Il n'y a pas de produit correspondant à votre recherche</span>
	</div>
{% endif %}
